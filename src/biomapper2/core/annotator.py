import logging
from typing import Dict, Any


def annotate(entity: Dict[str, Any]) -> Dict[str, Any]:
    logging.debug(f"Beginning annotation step..")
    # TODO: add in different annotation methods..

    return entity
